{
  "name": "vbkpharma-amrita",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix client run dev -- --host 0.0.0.0",
    "build": "npm --prefix client run build",
    "preview": "npm --prefix client run preview -- --host 0.0.0.0"
  }
}
